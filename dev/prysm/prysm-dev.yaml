participants:
  # On a network with a single node, Prysm currently fails to run while it searches for peers.
  # panic: runtime error: invalid memory address or nil pointer dereference
  - cl_type: prysm
    cl_image: prysm-beacon-chain-dev
    vc_type: prysm
    vc_image: prysm-validator-dev
    el_type: geth
